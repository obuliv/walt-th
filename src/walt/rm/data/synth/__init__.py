"""Spider-based synthetic RM training data: real per-database SQLite schemas paired with
deterministic, sqlglot-AST-based sql_bad corruptions (no LLM involved). See
build_synth_dataset.py for the CLI entry point.
"""
